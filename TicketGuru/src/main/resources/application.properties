
#
spring.jpa.show-sql=true
spring.jackson.serialization.fail-on-empty-beans=false

#chance profile between local/heroku. 
#Local when testing locally with h2. Heroku when pushing to git so that heroku application runs properly.
spring.profiles.default=heroku   
